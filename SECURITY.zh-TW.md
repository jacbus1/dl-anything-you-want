# 安全說明

[English](SECURITY.md) | **繁體中文**

DL Anything You Want 0.1.x 是研究原型，並非經獨立安全審計的公開服務；不承諾穩定版支援或漏洞回應時限。

## 漏洞回報

不要在公開 issue 貼出憑證、有效下載票證、私人媒體或漏洞利用細節。只有在擁有者啟用私人漏洞回報後，才使用 repo 的 **Security → Report a vulnerability**。本次變更沒有驗證或設定私人回報渠道／安全電郵。若渠道不可用，只要求維護者啟用，不要公開漏洞內容。正式公開服務前須設定私人渠道。

## 已實作的邊界

- 來源只接受指定 Instagram／Threads HTTPS 貼文 host 及路徑，不提供任意網址代理、登入或 cookies／session 上傳。
- 媒體只接受 Meta CDN 網域後綴，或指定 Cobalt origin 的精確 `/tunnel` 路徑。非信任來源在 socket DNS lookup 時驗證公開 IPv4；重新導向有次數限制並逐次核對。不支援只有 IPv6 的目的地。
- Cobalt 是受信任的**營運者設定**，可使用內網 HTTP／私人位址，不能讓終端使用者指定。此專案沒有審計 Cobalt 內部行為。
- Threads 只解析 JSON，不執行來源腳本；核對貼文 ID，OG 後備解析亦要求 canonical／OG URL 相符。無法確認時停止。
- 請求、HTML、JSON 有大小限制；檔案串流預設 100 MiB，上限可設至 500 MiB；每程序最多同時兩個解析及兩個檔案傳輸。
- 記憶體固定時間窗限流：每 socket IP 每分鐘六次解析、全局每分鐘 30 次解析、每 IP 每分鐘 24 次檔案請求；不信任 `X-Forwarded-For`。
- 隨機單次票證 60 秒後不可使用，最多 500 筆。過期資料在操作時清理，不保證第 60 秒即實體刪除；重啟會遺失票證。
- 只接受 JPEG／PNG／WebP／MP4 MIME，檔名由程式產生。MIME 檢查不是完整媒體解碼或惡意檔案掃描。
- 應用程式只串流媒體，不寫入媒體庫，沒有歷史資料庫或追蹤分析。這不代表主機、gateway、瀏覽器或 Cobalt 都沒有日誌。

## 公開營運前

使用 HTTPS、經審查的 gateway 身分驗證／防濫用控制、連線／時間／請求大小限制、流量預算及進出網絡限制。CORS 和勾選框不是身分驗證或權利證明；非瀏覽器客戶端可偽造 Origin，附帶限流亦不是 DDoS 防護。

反向代理可能令所有使用者共用同一 socket IP；應設定邊緣限流，不要信任任意轉發標頭。多副本須共享票證／限流儲存，或有明確路由設計；目前是單程序原型。

核對並鎖定 runtime、容器、Cobalt 及 Actions 版本，掃描映像／依賴，避免秘密資料進入資產或 Git 歷史。CI 是本機程式測試，不是上游安全審計。須設定回報／下架處理並核對平台、主機及內容規則。

不提供 Range／續傳、轉檔、CAPTCHA 繞過、限流規避或私人帳號存取。標頭送出後才超出大小限制的串流會被中斷，瀏覽器須把不完整檔案視為失敗。宣稱服務上線前，完成 [部署](docs/zh-TW/deployment.md)及 [驗證](docs/zh-TW/verification.md)。
